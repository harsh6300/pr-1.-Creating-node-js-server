const add = (x, y) => {
    return x + y;
}
const min = (x, y) => {
    return x - y;
}
const mul = (x, y) => {
    return x * y;
}
const div = (x, y) => {
    return x / y;
}

module.exports = {
    add, min, mul, div
}